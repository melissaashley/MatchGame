"use strict";

var grabCards = document.getElementsByClassName('tile');
var cards = Array.from(grabCards);
var flippedCards = []; // Empty Card Array after clicks

var emptyCards = function emptyCards() {
  flippedCards = [];
}; // Card does not match


var cardNotMatched = function cardNotMatched() {
  console.log('rude, no match');
  emptyCards();
}; // Card is a match


var cardMatched = function cardMatched() {
  console.log('it matches');
  emptyCards();
}; // Check if card matches


var checkMatch = function checkMatch(match) {
  flippedCards.push(match.getAttribute('data-type'));
  var cardLength = flippedCards.length;
  var cardCount = 2;
  console.log(flippedCards);

  if (cardCount === cardLength) {
    if (flippedCards[0] === flippedCards[1]) {
      cardMatched();
    } else {
      cardNotMatched();
    }
  }
}; // Card is selected


var flipCard = function flipCard() {
  var match = this;

  if (!match.classList.contains('flipped')) {
    match.classList.add('flipped');
    checkMatch(match);
  }
}; // Add Event Listener to each card


for (var _i = 0, _cards = cards; _i < _cards.length; _i++) {
  var eachCard = _cards[_i];
  eachCard.addEventListener('click', flipCard);
}
//# sourceMappingURL=app.min.js.map
